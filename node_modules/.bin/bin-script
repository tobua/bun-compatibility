#!/usr/bin/env bun
import { execSync } from 'node:child_process'

const isBun = typeof Bun !== 'undefined'

console.log(`Running with ${isBun ? 'Bun' : 'Node.js'} in ${process.cwd()}.`)

console.log('run', typeof Bun !== 'undefined', process.cwd())

execSync('node another.js', { stdio: 'inherit' })